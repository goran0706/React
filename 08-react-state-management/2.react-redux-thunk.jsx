import React from 'react'

/**
    Redux - The redux library
    React-Redux - The integration layer between React and Redux
    React-Thunk - The middleware to help use make requests in a redux application
    axios - The library that helps us make network requests

    - Components are generally responsible for fetching data they need by calling an action creator.
    - Action creators are responsible for making API requests.
    - We get fetched data into a component by generating new state in our redux store, then getting that into our component through mapStateToProps.
 */

const ReactReduxThunk = () => {
  return <div>16.ReactReduxThunk</div>
}

export default ReactReduxThunk
